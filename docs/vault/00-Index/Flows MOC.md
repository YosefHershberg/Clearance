---
title: Flows MOC
type: moc
tags:
  - moc
  - flow
---

# Flows MOC

End-to-end journeys spanning [[Client MOC|client]] and [[Server MOC|server]].

- [[Authentication Flow]] — how a request gets `req.user` attached
- [[Request Lifecycle]] — middleware → controller → response

> [!info] Feature flows missing
> No domain features exist yet, so there are no per-feature flows (e.g. "submit clearance request"). Add one note per flow as features land.
